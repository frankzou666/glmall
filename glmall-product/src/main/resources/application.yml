server:
  port: 9400

spring:
  application:
    name: glmall-product
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.146:8848
        namespace: glmall
        group: glmall

      config:
        server-addr: 192.168.1.146:8848
        namespace: glmall
        group: glmall

  config:
    import:
      - optional:nacos:common.yaml?group=glmall&refreshEnabled=true   #项目公用配置
      - nacos:product.yaml?group=glmall&refreshEnabled=true    #子项目特殊配置
